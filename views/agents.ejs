<!doctype html>

<html lang="en" class="light-style layout-navbar-fixed layout-menu-fixed layout-compact" dir="ltr"
    data-theme="theme-default" data-assets-path="/" data-template="vertical-menu-template">

<head>
    <script>
        const url = `${window.location.protocol}//${window.location.host}`;
    </script>
    <meta charset="utf-8" />
    <meta name="viewport"
        content="width=device-width, initial-scale=1.0, user-scalable=no, minimum-scale=1.0, maximum-scale=1.0" />
    <meta name="msapplication-TileColor" content="#ffffff" />
    <meta name="msapplication-TileImage" content="/img/favicon/ms-icon-144x144.png" />
    <meta name="theme-color" content="#ffffff" />
    <link rel="apple-touch-icon" sizes="57x57" href="/img/favicon/apple-icon-57x57.png" />
    <link rel="apple-touch-icon" sizes="60x60" href="/img/favicon/apple-icon-60x60.png" />
    <link rel="apple-touch-icon" sizes="72x72" href="/img/favicon/apple-icon-72x72.png" />
    <link rel="apple-touch-icon" sizes="76x76" href="/img/favicon/apple-icon-76x76.png" />
    <link rel="apple-touch-icon" sizes="114x114" href="/img/favicon/apple-icon-114x114.png" />
    <link rel="apple-touch-icon" sizes="120x120" href="/img/favicon/apple-icon-120x120.png" />
    <link rel="apple-touch-icon" sizes="144x144" href="/img/favicon/apple-icon-144x144.png" />
    <link rel="apple-touch-icon" sizes="152x152" href="/img/favicon/apple-icon-152x152.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/img/favicon/apple-icon-180x180.png" />
    <link rel="icon" type="image/png" sizes="192x192" href="/img/favicon/android-icon-192x192.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/img/favicon/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="96x96" href="/img/favicon/favicon-96x96.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/img/favicon/favicon-16x16.png" />
    <!--<link rel="manifest" href="/img/favicon/manifest.json">-->

    <title>Agents | GoldenHealth</title>

    <meta name="description" content="" />

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
        href="https://fonts.googleapis.com/css2?family=Public+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&ampdisplay=swap"
        rel="stylesheet" />

    <!-- Icons -->
    <link rel="stylesheet" href="/vendor/fonts/fontawesome.css" />
    <link rel="stylesheet" href="/vendor/fonts/tabler-icons.css" />
    <link rel="stylesheet" href="/vendor/fonts/flag-icons.css" />

    <!-- Core CSS -->
    <link rel="stylesheet" href="/vendor/css/rtl/core.css" class="template-customizer-core-css" />
    <link rel="stylesheet" href="/vendor/css/rtl/theme-default.css" class="template-customizer-theme-css" />
    <link rel="stylesheet" href="/css/demo.css" />

    <!-- Vendors CSS -->
    <link rel="stylesheet" href="/vendor/libs/node-waves/node-waves.css" />
    <link rel="stylesheet" href="/vendor/libs/perfect-scrollbar/perfect-scrollbar.css" />
    <link rel="stylesheet" href="/vendor/libs/typeahead-js/typeahead.css" />
    <link rel="stylesheet" href="/vendor/libs/apex-charts/apex-charts.css" />
    <link rel="stylesheet" href="/vendor/libs/swiper/swiper.css" />
    <link rel="stylesheet" href="/vendor/libs/datatables-bs5/datatables.bootstrap5.css" />
    <link rel="stylesheet" href="/vendor/libs/datatables-responsive-bs5/responsive.bootstrap5.css" />
    <link rel="stylesheet" href="/vendor/libs/datatables-checkboxes-jquery/datatables.checkboxes.css" />
    <link rel="stylesheet" href="/vendor/libs/datatables-buttons-bs5/buttons.bootstrap5.css" />
    <link rel="stylesheet" href="/vendor/css/pages/cards-advance.css" />
    <link rel="stylesheet" href="/vendor/libs/datatables-rowgroup-bs5/rowgroup.bootstrap5.css" />
    <script src="/vendor/js/helpers.js"></script>

    <script src="/js/config.js"></script>
</head>

<body>
    <div class="layout-wrapper layout-content-navbar">
        <div class="layout-container">
            <%- include('partials/sidebar', { user: user, activePage: activePage }) %>
                <div class="layout-page">
                    <%- include('partials/navbar', {user: user}) %>
                        <div class="content-wrapper">
                            <div class="container-xxl flex-grow-1 container-p-y">
                                <!-- DataTable with Buttons -->
                                <div class="card">
                                    <div class="card-datatable table-responsive pt-0">
                                        <div id="DataTables_Table_0_wrapper"
                                            class="dataTables_wrapper dt-bootstrap5 no-footer">
                                            <div class="card-header flex-column flex-md-row">
                                                <div class="head-label text-center">
                                                    <h5 class="card-title mb-0">Agents</h5>
                                                </div>
                                                <div class="dt-action-buttons text-end pt-6 pt-md-0">
                                                    <div class="d-flex justify-content-end flex-wrap gap-2">
                                                        <% if (user && user.rights && user.rights.includes(2)) { %>
                                                            <button class="btn btn-success waves-effect waves-light"
                                                                type="button" data-bs-toggle="modal"
                                                                data-bs-target="#addAgentModal">
                                                                <i class="ti ti-user-plus me-sm-1"></i>
                                                                <span class="d-none d-sm-inline-block">Add Agent</span>
                                                            </button>

                                                            <button
                                                                class="btn btn-secondary create-new btn-primary waves-effect waves-light"
                                                                type="button" data-bs-toggle="offcanvas"
                                                                data-bs-target="#add-new-record">
                                                                <i class="ti ti-plus me-sm-1"></i>
                                                                <span class="d-none d-sm-inline-block">Onboarding</span>
                                                            </button>
                                                            <% } %>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="row">
                                                <div class="col-sm-12 col-md-6">
                                                    <div class="dataTables_length" id="DataTables_Table_0_length">
                                                        <label>Show
                                                            <select name="DataTables_Table_0_length"
                                                                aria-controls="DataTables_Table_0" class="form-select">
                                                                <option value="25" selected>25</option>
                                                                <option value="50">50</option>
                                                                <option value="75">75</option>
                                                                <option value="100">100</option>
                                                            </select>
                                                            entries</label>
                                                    </div>
                                                </div>
                                                <div
                                                    class="col-sm-12 col-md-6 d-flex justify-content-center justify-content-md-end mt-n6 mt-md-0">
                                                    <div id="DataTables_Table_0_filter" class="dataTables_filter">
                                                        <label>Search:<input type="search" class="form-control"
                                                                placeholder=""
                                                                aria-controls="DataTables_Table_0" /></label>
                                                    </div>
                                                </div>
                                            </div>
                                            <table class="datatables-basic table dataTable no-footer dtr-column"
                                                id="DataTables_Table_0" aria-describedby="DataTables_Table_0_info"
                                                style="width: 1396px">
                                                <thead>
                                                    <tr>
                                                        <th class="sorting_disabled" aria-controls="DataTables_Table_0"
                                                            rowspan="1" colspan="1" style="width: 30px" aria-label="#">#
                                                        </th>
                                                        <th class="sorting_disabled dtr-hidden"
                                                            aria-controls="DataTables_Table_0" rowspan="1" colspan="1"
                                                            style="width: 10px" aria-label=""></th>
                                                        <th class="sorting_disabled" tabindex="0"
                                                            aria-controls="DataTables_Table_0" rowspan="1" colspan="1"
                                                            style="width: 144px"
                                                            aria-label="Name: activate to sort column ascending">
                                                            Name
                                                        </th>
                                                        <th class="sorting_disabled" tabindex="1"
                                                            aria-controls="DataTables_Table_0" rowspan="1" colspan="1"
                                                            style="width: 147px"
                                                            aria-label="Email: activate to sort column ascending">
                                                            Email
                                                        </th>
                                                        <th class="sorting_disabled" tabindex="2"
                                                            aria-controls="DataTables_Table_0" rowspan="1" colspan="1"
                                                            style="width: 130px"
                                                            aria-label="Photo: activate to sort column ascending">
                                                            Agency
                                                        </th>
                                                        <th class="sorting_disabled" tabindex="3"
                                                            aria-controls="DataTables_Table_0" rowspan="1" colspan="1"
                                                            style="width: 130px"
                                                            aria-label="Created_at: activate to sort column ascending">
                                                            Created at
                                                        </th>
                                                        <th class="sorting_disabled" rowspan="1" colspan="1"
                                                            style="width: 174px" aria-label="Actions">
                                                            Actions
                                                        </th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    <tr class="odd">
                                                        <td valign="top" colspan="6" class="dataTables_empty">
                                                            Loading...
                                                        </td>
                                                    </tr>
                                                </tbody>
                                            </table>
                                            <div class="row">
                                                <div class="col-sm-12 col-md-6">
                                                    <div class="dataTables_info" id="DataTables_Table_0_info"
                                                        role="status" aria-live="polite">
                                                        Showing 0 to 0 of 0 entries
                                                    </div>
                                                </div>
                                                <div class="col-sm-12 col-md-6">
                                                    <div class="dataTables_paginate paging_simple_numbers"
                                                        id="DataTables_Table_0_paginate">
                                                        <ul class="pagination">
                                                            <li class="paginate_button page-item previous disabled"
                                                                id="DataTables_Table_0_previous">
                                                                <a aria-controls="DataTables_Table_0"
                                                                    aria-disabled="true" role="link"
                                                                    data-dt-idx="previous" tabindex="-1"
                                                                    class="page-link"><i
                                                                        class="ti ti-chevron-left ti-sm"></i></a>
                                                            </li>
                                                            <li class="paginate_button page-item next disabled"
                                                                id="DataTables_Table_0_next">
                                                                <a aria-controls="DataTables_Table_0"
                                                                    aria-disabled="true" role="link" data-dt-idx="next"
                                                                    tabindex="-1" class="page-link"><i
                                                                        class="ti ti-chevron-right ti-sm"></i></a>
                                                            </li>
                                                        </ul>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <!-- Modal to send onboarding -->
                                <div class="offcanvas offcanvas-end" id="add-new-record">
                                    <div class="offcanvas-header border-bottom">
                                        <h5 class="offcanvas-title" id="exampleModalLabel">
                                            Send Email
                                        </h5>
                                        <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas"
                                            aria-label="Close"></button>
                                    </div>
                                    <div class="offcanvas-body flex-grow-1">
                                        <form class="add-new-record pt-2 pb-2 px-1 row g-3" id="form-send-email"
                                            onsubmit="return false">
                                            <div class="col-12 mb-3">
                                                <label class="form-label" for="email">Email</label>
                                                <div class="input-group input-group-merge">
                                                    <span id="basicFullname2" class="input-group-text"><i
                                                            class="ti ti-user"></i></span>
                                                    <input type="email" id="email" class="form-control dt-full-name"
                                                        name="email" placeholder="example@mail.com"
                                                        aria-label="example@mail.com" aria-describedby="email" />
                                                </div>
                                            </div>
                                            <div class="col-6 mb-3">
                                                <label class="form-label" for="firstName">First Name</label>
                                                <input type="text" id="firstName" class="form-control" name="firstName"
                                                    placeholder="First Name" required />
                                            </div>
                                            <div class="col-6 mb-3">
                                                <label class="form-label" for="lastName">Last Name</label>
                                                <input type="text" id="lastName" class="form-control" name="lastName"
                                                    placeholder="Last Name" required />
                                            </div>
                                            <div class="col-12 mb-3">
                                                <label class="form-label mb-2">Required Documents</label>
                                                <div class="row g-2">
                                                    <div class="col-6">
                                                        <div class="form-check mb-2">
                                                            <input class="form-check-input" type="checkbox"
                                                                id="directDepositForm" name="directDepositForm">
                                                            <label class="form-check-label"
                                                                for="directDepositForm">Direct
                                                                Deposit Form</label>
                                                        </div>
                                                        <div class="form-check mb-2">
                                                            <input class="form-check-input" type="checkbox"
                                                                id="driverLicense" name="driverLicense">
                                                            <label class="form-check-label" for="driverLicense">Driver
                                                                License</label>
                                                        </div>
                                                        <div class="form-check mb-2">
                                                            <input class="form-check-input" type="checkbox"
                                                                id="EINconfirmation" name="EINconfirmation">
                                                            <label class="form-check-label" for="EINconfirmation">EIN
                                                                Confirmation</label>
                                                        </div>
                                                        <div class="form-check mb-2">
                                                            <input class="form-check-input" type="checkbox" id="eANDo"
                                                                name="eANDo">
                                                            <label class="form-check-label" for="eANDo">E&O</label>
                                                        </div>
                                                        <div class="form-check mb-2">
                                                            <input class="form-check-input" type="checkbox"
                                                                id="proLicense" name="proLicense">
                                                            <label class="form-check-label"
                                                                for="proLicense">Professional
                                                                License</label>
                                                        </div>
                                                        <div class="form-check mb-2">
                                                            <input class="form-check-input" type="checkbox"
                                                                id="taxIdentificationW9" name="taxIdentificationW9"
                                                                checked disabled>
                                                            <label class="form-check-label" for="taxIdentificationW9">
                                                                Tax Identification W9
                                                            </label>
                                                        </div>
                                                    </div>
                                                    <div class="col-6">
                                                        <div class="form-check mb-2">
                                                            <input class="form-check-input" type="checkbox"
                                                                id="ffmCertification" name="ffmCertification">
                                                            <label class="form-check-label" for="ffmCertification">FFM
                                                                Certification</label>
                                                        </div>
                                                        <div class="form-check mb-2">
                                                            <input class="form-check-input" type="checkbox"
                                                                id="gaLicense" name="gaLicense">
                                                            <label class="form-check-label" for="gaLicense">GA
                                                                License</label>
                                                        </div>
                                                        <div class="form-check mb-2">
                                                            <input class="form-check-input" type="checkbox"
                                                                id="gaCertification" name="gaCertification">
                                                            <label class="form-check-label" for="gaCertification">GA
                                                                Certification</label>
                                                        </div>
                                                        <div class="form-check mb-2">
                                                            <input class="form-check-input" type="checkbox" id="sunbiz"
                                                                name="sunbiz">
                                                            <label class="form-check-label" for="sunbiz">Sunbiz</label>
                                                        </div>
                                                        <div class="form-check mb-2">
                                                            <input class="form-check-input" type="checkbox"
                                                                id="corporationLicense" name="corporationLicense">
                                                            <label class="form-check-label"
                                                                for="corporationLicense">Corporation
                                                                License</label>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="col-12 d-flex justify-content-start gap-2 mt-3">
                                                <button type="submit" class="btn btn-primary data-submit me-sm-4 me-1">
                                                    Send
                                                </button>
                                                <button type="reset" class="btn btn-outline-secondary"
                                                    data-bs-dismiss="offcanvas">
                                                    Cancel
                                                </button>
                                            </div>
                                        </form>
                                    </div>
                                </div>
                                <!--/ Modal to send onboarding -->
                                <!-- Modal to add agent -->
                                <div class="modal fade" id="addAgentModal" tabindex="-1"
                                    aria-labelledby="addAgentModalLabel" aria-hidden="true">
                                    <div class="modal-dialog modal-dialog-centered">
                                        <div class="modal-content">

                                            <div class="modal-header">
                                                <h5 class="modal-title" id="addAgentModalLabel">Add New Agent</h5>
                                                <button type="button" class="btn-close" data-bs-dismiss="modal"
                                                    aria-label="Close"></button>
                                            </div>

                                            <form id="addAgentForm">
                                                <div class="modal-body">

                                                    <!-- Email -->
                                                    <div class="mb-3">
                                                        <label for="agentEmail" class="form-label">Email</label>
                                                        <input type="email" class="form-control" id="agentEmail"
                                                            name="email" required placeholder="example@email.com">
                                                    </div>

                                                    <!-- Legal Name -->
                                                    <div class="mb-3">
                                                        <label for="agentLegalName" class="form-label">Legal
                                                            Name</label>
                                                        <input type="text" class="form-control" id="agentLegalName"
                                                            name="legalName" required placeholder="John Doe">
                                                    </div>

                                                    <!-- Phone -->
                                                    <div class="mb-3">
                                                        <label for="agentPhone" class="form-label">Phone (US)</label>
                                                        <input type="tel" class="form-control" id="agentPhone"
                                                            name="phone" required placeholder="(123) 456-7890"
                                                            maxlength="14">
                                                    </div>

                                                    <!-- NPN -->
                                                    <div class="mb-3">
                                                        <label for="agentNpn" class="form-label">NPN</label>
                                                        <input type="text" class="form-control" id="agentNpn" name="npn"
                                                            required placeholder="123456" maxlength="10">
                                                    </div>

                                                    <!-- Subordination -->
                                                    <div class="mb-3">
                                                        <label for="agentSubordination"
                                                            class="form-label">Subordination</label>
                                                        <select class="form-select" id="agentSubordination"
                                                            name="subordination" required>
                                                            <option value="" disabled selected>Select location or agency
                                                            </option>
                                                            <% allAgencies.forEach(a=> { %>
                                                                <option
                                                                    value="<%= a.isAgency ? `agency-${a.id}` : `franchise-${a.id}` %>">
                                                                    <%= a.name %>
                                                                </option>
                                                                <% }) %>
                                                        </select>
                                                    </div>

                                                </div>
                                                <div class="modal-footer">
                                                    <button type="button" class="btn btn-secondary"
                                                        data-bs-dismiss="modal">Cancel</button>
                                                    <button type="submit" class="btn btn-success">Save</button>
                                                </div>
                                            </form>

                                        </div>
                                    </div>
                                </div>

                                <script>
                                    // Phone formatting: (XXX) XXX-XXXX
                                    const phoneInput = document.getElementById("agentPhone");
                                    phoneInput.addEventListener("keypress", function (e) {
                                        if (!/[0-9]/.test(e.key)) {
                                            e.preventDefault();
                                        }
                                    });

                                    phoneInput.addEventListener("input", function (e) {
                                        let value = e.target.value.replace(/\D/g, "");
                                        if (value.length > 10) value = value.slice(0, 10);

                                        if (value.length > 6) {
                                            e.target.value = `(${value.slice(0, 3)}) ${value.slice(3, 6)}-${value.slice(6)}`;
                                        } else if (value.length > 3) {
                                            e.target.value = `(${value.slice(0, 3)}) ${value.slice(3)}`;
                                        } else if (value.length > 0) {
                                            e.target.value = `(${value}`;
                                        }
                                    });

                                    document.getElementById("agentNpn").addEventListener("input", function (e) {
                                        e.target.value = e.target.value.replace(/[^0-9]/g, "");
                                    });
                                    document.getElementById("addAgentForm").addEventListener("submit", function (e) {
                                        phoneInput.value = phoneInput.value.replace(/\D/g, "");
                                    });
                                </script>
                                <!--/ Modal to add agent -->
                                <!--/ DataTable with Buttons -->
                            </div>
                            <div class="content-backdrop fade"></div>
                        </div>
                </div>
        </div>
        <div class="layout-overlay layout-menu-toggle"></div>

        <!-- Drag Target Area To SlideIn Menu On Small Screens -->
        <div class="drag-target"></div>
    </div>

    <!-- Overlay Spinner -->
    <div id="email-overlay"
        style="display:none;position:fixed;z-index:9999;top:0;left:0;width:100vw;height:100vh;background:rgba(255,255,255,0.7);justify-content:center;align-items:center;">
        <div class="spinner-border text-primary" role="status" style="width:3rem;height:3rem;">
            <span class="visually-hidden">Sending...</span>
        </div>
    </div>

    <div class="bs-toast toast toast-ex animate__animated my-2 animate__slideInDown toast-ex1" role="alert"
        aria-live="assertive" aria-atomic="true" data-bs-delay="3000">
        <div class="toast-header"> <i class="ti ti-bell ti-xs me-2"></i>
            <div class="me-auto fw-medium text-primary">GoldenHealth</div> <small class="text-warning">Agents</small>
            <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
        <div class="toast-body text-primary"> You should receive an email, please check it. </div>
    </div>

    <script src="/vendor/libs/jquery/jquery.js"></script>
    <script src="/vendor/libs/popper/popper.js"></script>
    <script src="/vendor/js/bootstrap.js"></script>
    <script src="/vendor/libs/node-waves/node-waves.js"></script>
    <script src="/vendor/libs/perfect-scrollbar/perfect-scrollbar.js"></script>
    <script src="/vendor/libs/hammer/hammer.js"></script>
    <script src="/vendor/libs/i18n/i18n.js"></script>
    <script src="/vendor/libs/typeahead-js/typeahead.js"></script>
    <script src="/vendor/js/menu.js"></script>

    <!-- endbuild -->

    <!-- Vendors JS -->
    <script src="/vendor/libs/datatables-bs5/datatables-bootstrap5.js"></script>
    <!-- Flat Picker -->
    <script src="/vendor/libs/moment/moment.js"></script>
    <script src="/vendor/libs/flatpickr/flatpickr.js"></script>
    <!-- Form Validation -->
    <script src="/vendor/libs/@form-validation/popular.js"></script>
    <script src="/vendor/libs/@form-validation/bootstrap5.js"></script>
    <script src="/vendor/libs/@form-validation/auto-focus.js"></script>

    <!-- Main JS -->
    <script src="/js/main.js"></script>

    <script id="users-data" type="application/json">
      <%- JSON.stringify(registeredUsers) %>
    </script>
    <script>
        function showToast(headerText, bodyText) {
            const toast = document.querySelector('.toast-ex1');
            toast.querySelector('.toast-header small').textContent = headerText;
            toast.querySelector('.toast-body').textContent = bodyText;
            const bsToast = new bootstrap.Toast(toast);
            bsToast.show();
        }
    </script>

    <script>
        function renderTable(users, page, pageSize, searchTerm = "") {
            const table = document.querySelector(".datatables-basic");
            const tbody = table.querySelector("tbody");
            tbody.innerHTML = "";

            const filtered = users.filter(u =>
                (u.display_name || "").toLowerCase().includes(searchTerm.toLowerCase()) ||
                (u.email || "").toLowerCase().includes(searchTerm.toLowerCase())
            );

            const start = (page - 1) * pageSize;
            const end = start + pageSize;
            const pageUsers = filtered.slice(start, end);

            if (pageUsers.length === 0) {
                const tr = document.createElement("tr");
                tr.innerHTML = `<td valign="top" colspan="7" class="dataTables_empty">No users found.</td>`;
                tbody.appendChild(tr);
            } else {
                pageUsers.forEach((user, idx) => {
                    const tr = document.createElement("tr");

                    const tdIndex = document.createElement("td");
                    tdIndex.textContent = start + idx + 1;
                    tr.appendChild(tdIndex);

                    const tdAvatar = document.createElement("td");
                    tdAvatar.className = "dtr-hidden";

                    const getInitials = (name) => {
                        if (!name) return '??';
                        const parts = name.trim().split(' ');
                        if (parts.length >= 2) {
                            return (parts[0][0] + parts[parts.length - 1][0]).toUpperCase();
                        }
                        return name.substring(0, 2).toUpperCase();
                    };

                    const photoSrc = user.photoPath
                        ? (user.photoPath.startsWith('http') ? user.photoPath : `${url}/${user.photoPath}`)
                        : null;

                    tdAvatar.innerHTML = photoSrc
                        ? `<img src="${photoSrc}" alt="" style="width:2.5rem;height:2.5rem;border-radius:50%;object-fit:cover;">`
                        : `<span class="avatar-initial rounded-circle bg-label-primary d-inline-flex align-items-center justify-content-center" style="width:2.5rem;height:2.5rem;">${getInitials(user.display_name || user.email)}</span>`;
                    tr.appendChild(tdAvatar);


                    const tdName = document.createElement("td");
                    tdName.textContent = user.display_name || "";
                    tr.appendChild(tdName);

                    const tdEmail = document.createElement("td");
                    tdEmail.innerHTML = user.email ? `<a href="mailto:${user.email}">${user.email}</a>` : "";
                    tr.appendChild(tdEmail);

                    const tdAgency = document.createElement("td");
                    tdAgency.textContent = user.agency;
                    tr.appendChild(tdAgency);

                    const tdCreated = document.createElement("td");
                    tdCreated.textContent = user.createdAt
                        ? new Date(user.createdAt).toLocaleDateString()
                        : "";
                    tr.appendChild(tdCreated);

                    const tdAction = document.createElement("td");
                    tdAction.innerHTML = `
    <a href="/users/profile/${user.user_id}" class="ti ti-search me-2" title="View Profile"></a>
    <a href="javascript:void(0);" class="ti ti-trash text-danger" title="Delete User"
       onclick="if(confirm('Are you sure you want to delete this agent?')) { fetch('/users/agents/${user.user_id}', { method: 'DELETE' }).then(() => location.reload()); }">
`;
                    tr.appendChild(tdAction);

                    tbody.appendChild(tr);
                });
            }
            const info = document.getElementById("DataTables_Table_0_info");
            const showingStart = filtered.length === 0 ? 0 : start + 1;
            const showingEnd = Math.min(end, filtered.length);
            info.textContent = `Showing ${showingStart} to ${showingEnd} of ${filtered.length} entries`;

            renderPagination(filtered.length, page, pageSize);
        }

        function renderPagination(total, page, pageSize) {
            const ul = document.querySelector("#DataTables_Table_0_paginate ul.pagination");
            ul.innerHTML = "";

            const totalPages = Math.ceil(total / pageSize);

            // Prev
            const prevLi = document.createElement("li");
            prevLi.className = `paginate_button page-item previous${page === 1 ? " disabled" : ""}`;
            prevLi.innerHTML = `<a class="page-link" href="#" tabindex="-1"><i class="ti ti-chevron-left ti-sm"></i></a>`;
            prevLi.onclick = (e) => {
                e.preventDefault();
                if (page > 1) setPage(page - 1);
            };
            ul.appendChild(prevLi);

            // Números de página (máximo 5)
            let startPage = Math.max(1, page - 2);
            let endPage = Math.min(totalPages, startPage + 4);
            if (endPage - startPage < 4) startPage = Math.max(1, endPage - 4);

            for (let i = startPage; i <= endPage; i++) {
                const li = document.createElement("li");
                li.className = `paginate_button page-item${i === page ? " active" : ""}`;
                li.innerHTML = `<a class="page-link" href="#">${i}</a>`;
                li.onclick = (e) => {
                    e.preventDefault();
                    setPage(i);
                };
                ul.appendChild(li);
            }

            // Next
            const nextLi = document.createElement("li");
            nextLi.className = `paginate_button page-item next${page === totalPages || totalPages === 0 ? " disabled" : ""}`;
            nextLi.innerHTML = `<a class="page-link" href="#"><i class="ti ti-chevron-right ti-sm"></i></a>`;
            nextLi.onclick = (e) => {
                e.preventDefault();
                if (page < totalPages) setPage(page + 1);
            };
            ul.appendChild(nextLi);
        }

        // Estado global de paginado y búsqueda
        let users = [];
        let currentPage = 1;
        let pageSize = 25;
        let searchTerm = "";

        function setPage(page) {
            currentPage = page;
            renderTable(users, currentPage, pageSize, searchTerm);
        }

        function setPageSize(size) {
            pageSize = size;
            currentPage = 1;
            renderTable(users, currentPage, pageSize, searchTerm);
        }

        function setSearch(term) {
            searchTerm = term;
            currentPage = 1;
            renderTable(users, currentPage, pageSize, searchTerm);
        }

        document.addEventListener("DOMContentLoaded", () => {
            users = JSON.parse(document.getElementById("users-data").textContent);

            // Inicializar tabla
            renderTable(users, currentPage, pageSize);

            // Selector de cantidad
            document.querySelector('select[name="DataTables_Table_0_length"]').addEventListener("change", function () {
                setPageSize(Number(this.value));
            });

            // Barra de búsqueda
            document.querySelector('#DataTables_Table_0_filter input[type="search"]').addEventListener("input", function () {
                setSearch(this.value);
            });
        });
    </script>

    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const form = document.getElementById("addAgentForm");

            form.addEventListener("submit", async (e) => {
                e.preventDefault();

                if (!form.checkValidity()) {
                    form.classList.add("was-validated");
                    return;
                }

                const email = document.getElementById("agentEmail").value.trim();
                let users = JSON.parse(document.getElementById("users-data").textContent);

                const userExists = users.some(u => u.email.toLowerCase() === email.toLowerCase());
                if (userExists) {
                    showToast("Error", "This user is already registered.");
                    return;
                }

                const legalName = document.getElementById("agentLegalName").value.trim();
                const phone = document.getElementById("agentPhone").value.trim();
                const npn = document.getElementById("agentNpn").value.trim();
                const subordination = document.getElementById("agentSubordination").value;

                if (!subordination) {
                    showToast("Error", "Please select a subordination");
                    return;
                }

                // Parse "agency-5" or "franchise-12"
                const [subType, ...subIdParts] = subordination.split("-");
                const subId = subIdParts.join("-");

                const payload = {
                    email,
                    legalName,
                    phone,
                    npn,
                    agency: subType === "agency" ? subId : null,
                    franchise: subType === "franchise" ? parseInt(subId) : null,
                };

                try {
                    const response = await fetch("/users/agents", {
                        method: "POST",
                        headers: {
                            "Content-Type": "application/json",
                        },
                        body: JSON.stringify(payload),
                    });

                    const result = await response.json();

                    if (!response.ok) {
                        const errorMessage = result.message || "Error adding the new agent";
                        showToast("Error", errorMessage);
                        return;
                    }

                    showToast("Success", "The agent was added")

                    const modalEl = document.getElementById("addAgentModal");
                    const modal = bootstrap.Modal.getInstance(modalEl);
                    modal.hide();

                    form.reset();
                    form.classList.remove("was-validated");

                    location.reload();
                } catch (error) {
                    const errorMessage = error.message || "Error adding the new agent";
                    showToast("Error", errorMessage);
                }
            });
        });
    </script>

    <script>
        document.addEventListener("DOMContentLoaded", () => {

            const toastAnimation1 = document.querySelector('.toast-ex1');
            const toastAnimation2 = document.querySelector('.toast-ex2');

            // Email form submit
            document
                .getElementById("form-send-email")
                .addEventListener("submit", async function (e) {
                    e.preventDefault();
                    const emailInput = document.getElementById("email");
                    const email = emailInput.value.trim();
                    const firstName = document.getElementById("firstName").value.trim();
                    const lastName = document.getElementById("lastName").value.trim();

                    if (!email) {
                        showToast("Error", "Please enter an email address.");
                        return;
                    }

                    let users = JSON.parse(document.getElementById("users-data").textContent);

                    const userExists = users.some(u => u.email.toLowerCase() === email.toLowerCase());
                    if (userExists) {
                        showToast("Error", "This user is already registered.");
                        return;
                    }

                    const docFields = [
                        "directDepositForm",
                        "driverLicense",
                        "EINconfirmation",
                        "eANDo",
                        "proLicense",
                        "taxIdentificationW9",
                        "ffmCertification",
                        "gaLicense",
                        "gaCertification",
                        "sunbiz",
                        "corporationLicense"
                    ];
                    const requiredDocuments = {};
                    docFields.forEach(field => {
                        // El Tax Identification W9 siempre es requerido
                        if (field === "taxIdentificationW9") {
                            requiredDocuments[field] = true;
                        } else {
                            requiredDocuments[field] = document.getElementById(field).checked;
                        }
                    });

                    const subject = "Create your account on GoldenHealth";

                    const overlay = document.getElementById("email-overlay");
                    overlay.style.display = "flex";

                    try {
                        const docsResponse = await fetch(`/necessaryDocs`, {
                            method: "POST",
                            headers: { "Content-Type": "application/json" },
                            body: JSON.stringify({
                                email,
                                ...requiredDocuments
                            }),
                        });

                        const docsData = await docsResponse.json();

                        if (!docsResponse.ok) {
                            const errorMessage = docsData.message || "Error adding necessary documents";
                            throw new Error(errorMessage);
                        }

                        const encryptEmail = await fetch(`/email/encrypt/${email}`, {
                            method: "POST",
                            headers: { "Content-Type": "application/json" },
                        });

                        const encryptedEmail = await encryptEmail.json();

                        if (!encryptEmail.ok) {
                            const errorMessage = encryptedEmail.message || "Error encrypting email";
                            throw new Error(errorMessage);
                        }

                        // Use firstName and lastName if available, otherwise use email
                        const fullName = [firstName, lastName].filter(Boolean).join(' ');
                        const displayName = fullName || email;
                        const signUpLink = `${url}/signUp/${encryptedEmail.data.encryptedData}`;

                        // Create HTML email body similar to Veterans Day template
                        const body = `
<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:v="urn:schemas-microsoft-com:vml" xmlns:o="urn:schemas-microsoft-com:office:office">
<head>
<!--[if gte mso 15]>
<xml>
<o:OfficeDocumentSettings>
<o:AllowPNG/>
<o:PixelsPerInch>96</o:PixelsPerInch>
</o:OfficeDocumentSettings>
</xml>
<![endif]-->
<meta charset="UTF-8"/>
<meta http-equiv="X-UA-Compatible" content="IE=edge"/>
<meta name="viewport" content="width=device-width, initial-scale=1"/>
<title>Create your account on GoldenHealth</title>
<style>
img{-ms-interpolation-mode:bicubic;}
table, td{mso-table-lspace:0pt;mso-table-rspace:0pt;}
p, a, li, td, blockquote{mso-line-height-rule:exactly;}
p, a, li, td, body, table, blockquote{-ms-text-size-adjust:100%;-webkit-text-size-adjust:100%;}
body{height:100%;margin:0;padding:0;width:100%;background:#f0f4f9;}
p{margin:0;padding:0;}
table{border-collapse:collapse;}
td, p, a{word-break:break-word;}
h1, h2, h3, h4, h5, h6{display:block;margin:0;padding:0;}
img, a img{border:0;height:auto;outline:none;text-decoration:none;}
body, #bodyTable{background-color:#f0f4f9;}
.mceText, .mceLabel{font-family:'Public Sans', Verdana, Geneva, sans-serif;color:#000;}
.mceText h1{color:#27388B;font-size:32px;font-weight:bold;line-height:1.25;text-align:center;margin:20px 0;}
.mceText p{color:#000;font-size:16px;line-height:1.5;text-align:center;margin:15px 0;}
.mceButton{background-color:#27388B;border-radius:5px;text-align:center;margin:20px 0;}
.mceButton a{color:#ffffff;font-size:16px;font-weight:bold;text-decoration:none;padding:12px 24px;display:inline-block;}
@media only screen and (max-width: 480px) {
body, table, td, p, a, li, blockquote{-webkit-text-size-adjust:none!important;}
body{width:100%!important;min-width:100%!important;}
.mceText h1{font-size:24px!important;}
.mceText p{font-size:14px!important;}
}
</style>
</head>
<body>
<center>
<table border="0" cellpadding="0" cellspacing="0" height="100%" width="100%" id="bodyTable" style="background-color:#f0f4f9;">
<tbody><tr>
<td align="center" valign="top" style="padding-top:40px;padding-bottom:40px;">
<table border="0" cellpadding="0" cellspacing="0" width="100%" style="max-width:660px;background-color:#ffffff;" role="presentation">
<tbody>
<!-- Logo Section -->
<tr>
<td style="padding:24px;text-align:center;">
<a href="https://goldentrust.com/" target="_blank">
<img src="${url}/img/branding/GoldenHealth-2.png" alt="GoldenHealth" style="max-width:300px;height:auto;display:block;margin:0 auto;"/>
</a>
</td>
</tr>
<!-- Main Content -->
<tr>
<td style="padding:0 24px 24px 24px;" class="mceText">
<h1>Welcome to GoldenHealth!</h1>
<p style="font-size:18px;margin-top:20px;">
Hello ${displayName},
</p>
<p>
We're excited to have you join our team. To get started and complete your registration, please click the button below to create your account.
</p>
<div class="mceButton" style="text-align:center;margin:30px 0;">
<a href="${signUpLink}" style="background-color:#27388B;color:#ffffff;padding:14px 28px;border-radius:5px;text-decoration:none;font-weight:bold;display:inline-block;">
Create Your Account
</a>
</div>
<p style="margin-top:30px;color:#666;">
Best regards,<br>
<strong>GoldenHealth Team</strong>
</p>
</td>
</tr>
<!-- Footer Section -->
<tr>
<td style="background-color:#27388B;padding:20px;text-align:center;">
<table border="0" cellpadding="0" cellspacing="0" width="100%" role="presentation">
<tbody>
<tr>
<td style="text-align:center;padding-bottom:10px;">
<a href="https://www.facebook.com/goldentrust" target="_blank" style="margin:0 8px;">
<img src="https://cdn-icons-png.flaticon.com/512/124/124010.png" alt="Facebook" width="24" height="24" style="display:inline-block;"/>
</a>
<a href="https://www.instagram.com/goldentrust" target="_blank" style="margin:0 8px;">
<img src="https://cdn-icons-png.flaticon.com/512/2111/2111463.png" alt="Instagram" width="24" height="24" style="display:inline-block;"/>
</a>
<a href="https://www.linkedin.com/company/goldentrust" target="_blank" style="margin:0 8px;">
<img src="https://cdn-icons-png.flaticon.com/512/174/174857.png" alt="LinkedIn" width="24" height="24" style="display:inline-block;"/>
</a>
</td>
</tr>
<tr>
<td style="color:#ffffff;font-size:12px;line-height:1.5;text-align:center;">
<p style="margin:5px 0;color:#ffffff;">
Golden Trust Insurance<br/>
Email: <a href="mailto:info@goldentrust.com" style="color:#ffffff;text-decoration:underline;">info@goldentrust.com</a><br/>
Phone: <a href="tel:+1234567890" style="color:#ffffff;text-decoration:underline;">+1 (234) 567-890</a>
</p>
<p style="margin:10px 0;color:#ffffff;font-size:11px;">
© 2025 Golden Trust Insurance. All rights reserved.
</p>
</td>
</tr>
</tbody>
</table>
</td>
</tr>
</tbody>
</table>
</td>
</tr>
</tbody>
</table>
</center>
</body>
</html>
`;

                        const response = await fetch(`/sendEmail/${email}`, {
                            method: "POST",
                            headers: { "Content-Type": "application/json" },
                            body: JSON.stringify({
                                email,
                                subject,
                                body
                            }),
                        });

                        const responseData = await response.json();

                        if (response.ok) {
                            showToast("Success", "Email sent successfully!");

                            const onboardingResponse = await fetch(`/onboardingSent/${email}`, {
                                method: "POST",
                                headers: { "Content-Type": "application/json" },
                                body: JSON.stringify({ firstName, lastName })
                            });

                            const onboardingData = await onboardingResponse.json();

                            if (!onboardingResponse.ok) {
                                const errorMessage = onboardingData.message || "Error recording onboarding";
                                showToast("Warning", errorMessage);
                            }

                            emailInput.value = "";
                            document.getElementById("firstName").value = "";
                            document.getElementById("lastName").value = "";
                            docFields.forEach(field => {
                                document.getElementById(field).checked = false;
                            });
                            const offcanvas = document.getElementById("add-new-record");
                            const bsOffcanvas = bootstrap.Offcanvas.getInstance(offcanvas);
                            if (bsOffcanvas) bsOffcanvas.hide();
                        } else {
                            showToast("Error", responseData.message || "Error sending email.");
                        }
                    } catch (err) {
                        const errorMessage = err.message || "Error sending email.";
                        showToast("Error", errorMessage);
                        console.log(err);
                    } finally {
                        overlay.style.display = "none";
                    }
                });
        });
    </script>
</body>

</html>